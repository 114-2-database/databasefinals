# NCCU 畢業離校檢核系統（通識／體育）專案核心介紹

## 專案定位與目標

本專案以「政大學生畢業前通識與體育學分檢核」為核心情境，打造一套可同時支援 **網頁互動檢核** 與 **API 查詢整合** 的系統。  
目標是將原本分散且複雜的畢業規則（領域下限、學分上限、核心通識跨領域、免修條件、體育門檻）轉換為可驗證、可追蹤、可視覺化的判定流程，讓學生與行政端能快速掌握是否達標與差距。

---

## 我們的專案特色

### 1) 規則導向的「可解釋」學分檢核引擎
- 將畢業條件拆成可計算規則：各類別最低／最高學分、總學分（不含體育）28、體育門數、核心通識跨領域要求等。
- 每一項結果都能回傳 `earned / counted / required / shortfall / met`，不只告訴你「有沒有過」，也清楚說明「差在哪裡」。

### 2) 跨領域課程可由使用者指定計入領域
- 針對同一門課可對應多領域（如人文/社會）的情境，前端允許學生選擇該課要計入哪個領域。
- 後端透過 `chosen_category` 接收並納入計算，避免跨領域課程被錯誤歸類。

### 3) 免修情境的真實化處理
- **資訊通識免修**：以系所規則（`computer_max == 0`）判定是否免修資訊領域。
- **英文免修**：不只是直接跳過，而是支援補修方案判定（大學外文同語種（一）＋（二），或兩門以上選修英文達規定學分），貼近實務規則。

### 4) 雙介面設計：Web + API 並行
- Web 版：Session + Jinja2，提供學生可操作的檢核流程（登入、課程明細、篩選、計算、結果視覺化）。
- API 版：JWT 驗證，提供系統整合與前後端分離擴充空間。

### 5) 可重現的測試資料生態
- 內建種子資料流程（科系、課程、學生、選課），可批次建立資料庫測試情境。
- 學生資料含 2000 筆規模，並可輸出測試帳密 CSV，便於展示與驗證。

---

## 核心邏輯（結案重點）

### A. 類別辨識與歸戶策略
1. 先解析課程 `remark`（支援中文/英文別名與多種分隔符）得到可計入類別。  
2. 若為跨領域課程且有 `chosen_category`，優先採用使用者選擇。  
3. 若未指定，預設採用 `remark` 第一個合法類別（避免學分遺失）。  
4. 若 `remark` 無法判定（例如外文通識），再以課名規則判斷英文／外文類別。

### B. 學分計算與上限控管
- 各領域先累計 `earned`，再套用系所上限得出 `counted`。
- 畢業總學分採「各類別 counted 加總」而非單純 earned，避免某類超修掩蓋其他不足。

### C. 核心通識跨領域檢核
- 僅計算人文、社會、自然三大領域。
- 需至少兩個不同領域各有核心通識課程，才算達標。

### D. 英文免修補修雙方案
- 對英文免修學生，外文/選修英文課程先收集、後驗證：
  - 方案一：同語種大學外文（一）＋（二）
  - 方案二：兩門以上不同選修英文且學分達標
- 兩案皆未達成時，仍保留部分可計入學分，確保結果對學生可解釋。

### E. 結果輸出
- 最終回傳類別狀態、體育狀態、核心通識狀態、總學分狀態與 `graduation_ready` 總判定，供前端渲染與 API 消費。

---

## 系統架構與技術選型

### 後端
- **FastAPI**：路由管理、依賴注入、API 建構。
- **SQLAlchemy ORM**：資料模型與查詢。
- **Alembic**：資料庫版本管理與遷移。
- **Pydantic Settings**：環境設定（資料庫連線、Session/JWT 參數）。

### 前端與呈現
- **Jinja2 Templates**：頁面模板渲染。
- **Bootstrap 5 + 自訂 CSS/JS**：儀表化視覺、篩選、排序、跨領域互動標籤。

### 驗證與安全
- **SessionMiddleware**：Web 登入狀態管理。
- **JWT**：API 存取權杖驗證。
- 密碼雜湊目前採 SHA-256（適合教學/demo），正式上線建議改為 `bcrypt/argon2`。

### 資料庫
- **MySQL**（依設定連線）
- 核心資料表：`departments`、`students`、`classes`、`selected_classes`

---

## 端到端流程（使用者視角）

1. 學生登入（Web Session 或 API JWT）。  
2. 系統讀取學生身份、主系/雙主修/輔系資訊與已通過課程。  
3. 在課程明細頁可篩選資料，並為跨領域課程指定計入領域。  
4. 送出計算後，檢核引擎依系所門檻逐項判定。  
5. 結果頁顯示各類達標狀況、核心通識規則、總學分進度與未達標缺口。  

---

## 專案價值與可擴充性

### 已達成價值
- 將高度規則化的畢業檢核流程程式化，降低人工判讀成本。
- 透過可解釋結果與視覺化介面，提升學生自我檢核效率。
- 提供 API 能力，支援後續與校務系統、行動端或 BI 報表整合。

### 後續優化方向
- 密碼機制升級為 `bcrypt/argon2`、補上更完整權限與審計紀錄。
- 補齊自動化測試（規則邊界、跨領域分配、免修情境）與 CI。
- 針對不同系所年度規則差異建立版本化規則引擎。
- 匯出個人化建議修課路徑（差幾學分、優先補哪一類）作為決策輔助。

---

## 結語

本專案的核心不只是「算學分」，而是將複雜、常變動的畢業規則轉為一套可維護、可驗證、可擴充的檢核平台。  
透過規則引擎 + 互動介面 + API 雙軌設計，專案已具備實務上線所需的架構雛型，也為後續擴充到更多學程與校務場景奠定基礎。
